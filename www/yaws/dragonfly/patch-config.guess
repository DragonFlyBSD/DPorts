--- config.guess.orig	2013-03-19 17:02:01.000000000 +0000
+++ config.guess
@@ -751,6 +751,9 @@ EOF
     *:BSD/OS:*:*)
 	echo ${UNAME_MACHINE}-unknown-bsdi${UNAME_RELEASE}
 	exit 0 ;;
+    *:DragonFly:*:*)
+	echo ${UNAME_MACHINE}-pc-dragonfly4
+	exit 0 ;;
     *:FreeBSD:*:*|*:GNU/FreeBSD:*:*)
 	# Determine whether the default compiler uses glibc.
 	eval $set_cc_for_build
