--- configure.in.orig	2013-11-04 18:26:06.000000000 +0000
+++ configure.in
@@ -256,7 +256,7 @@ case "$host_os" in
            AC_SUBST(FPIC)
            ;;
 
-        *bsd*)
+        *bsd*|*dragonfly*)
            AC_DEFINE(BSD)
            case "$host_os" in
                 freebsd*)
