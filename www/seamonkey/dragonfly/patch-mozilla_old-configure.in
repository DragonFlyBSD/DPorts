--- mozilla/old-configure.in.intermediate	2016-12-31 14:36:23 UTC
+++ mozilla/old-configure.in
@@ -1940,7 +1940,7 @@ then
     fi
 
     case "$target" in
-        *-*-freebsd*)
+        *-*-freebsd*|*-*-dragonfly*)
             AC_DEFINE(_REENTRANT)
             AC_DEFINE(_THREAD_SAFE)
             dnl -pthread links in -lpthread, so don't specify it explicitly.
