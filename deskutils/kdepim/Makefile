# $FreeBSD$

PORTNAME=	kdepim
DISTVERSION=	${KDE_APPLICATIONS_VERSION}
CATEGORIES=	deskutils kde

MAINTAINER=	kde@FreeBSD.org
COMMENT=	KDE PIM applications metaport

# USES=		kde:5 metaport
# USE_KDE=	${_USE_KDEPIM5_ALL}

IGNORE=		Requires www/qt5-webengine

.include <bsd.port.mk>
