--- CMakeLists.txt.orig	2019-03-23 07:16:59 UTC
+++ CMakeLists.txt
@@ -565,7 +565,7 @@ SET(BINDIR  bin)
 
 IF (NOT MANDIR AND NOT $ENV{MANDIR} STREQUAL "")
     SET (MANDIR "$ENV{MANDIR}")
-ELSEIF(CMAKE_SYSTEM_NAME STREQUAL "FreeBSD" OR CMAKE_SYSTEM_NAME STREQUAL "NetBSD")
+ELSEIF(CMAKE_SYSTEM_NAME STREQUAL "FreeBSD" OR CMAKE_SYSTEM_NAME STREQUAL "NetBSD" OR CMAKE_SYSTEM_NAME STREQUAL "DragonFly")
     SET (MANDIR "man")
 ELSE(NOT MANDIR AND NOT $ENV{MANDIR} STREQUAL "")
     SET (MANDIR "share/man")
