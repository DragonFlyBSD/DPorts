--- Play/config/configure.in.orig	2003-08-05 22:18:56.000000000 +0300
+++ Play/config/configure.in
@@ -8,6 +8,7 @@ case "$host" in
 *-sgi-*) hplay=sgi ;;
 *-next-*) hplay=NeXT; AC_CHECK_HEADERS(libc.h) ;;
 *-*-freebsd*) hplay=linux ;;
+*-*-dragonfly*) hplay=linux ;;
 *-*-linux*) hplay=linux ;;
 *-*-hpux*)  hplay=hpux ;;
 *-*-*) echo "Unknown host type $host" ;;
