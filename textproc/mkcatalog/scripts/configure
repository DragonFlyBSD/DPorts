#!/bin/sh
# $FreeBSD: textproc/mkcatalog/scripts/configure 300897 2012-07-14 14:29:18Z beat $
#

for i in ${CONFIGURE_TARGETS}
do
	if [ -f ${WRKDIR}/${i}.in ]; then
	cat ${WRKDIR}/${i}.in | /usr/bin/sed \
		-e "s;@@PREFIX@@;${PREFIX};g" \
	> ${WRKDIR}/${i}
	fi
done
