USES+=	ssl
CFLAGS += -DPTHREAD_MUTEX_ADAPTIVE_NP=PTHREAD_MUTEX_ERRORCHECK
CFLAGS += -I${OPENSSLINC}
LDFLAGS+= -L${OPENSSLLIB}

.if ${DFLYVERSION} > 500700
LIB_DEPENDS+=	libmd.so:security/libmd
.endif
