--- ext/libsass/Makefile.intermediate	2019-05-26 18:11:05.000000000 +0000
+++ ext/libsass/Makefile
@@ -128,12 +128,14 @@ ifeq ($(UNAME),Darwin)
 endif
 
 ifneq (Windows,$(UNAME))
+	ifneq (DragonFly,$(UNAME))
 	ifneq (FreeBSD,$(UNAME))
 		ifneq (OpenBSD,$(UNAME))
 			LDFLAGS += -ldl
 			LDLIBS += -ldl
 		endif
 	endif
+	endif
 endif
 
 ifneq ($(BUILD),shared)
