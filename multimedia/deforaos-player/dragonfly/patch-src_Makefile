--- src/Makefile.orig	2012-10-14 17:03:16.000000000 +0000
+++ src/Makefile
@@ -8,7 +8,7 @@ CPPFLAGS?=
 CFLAGSF	= -W `pkg-config --cflags libDesktop`
 CFLAGS	= -Wall -g -pedantic
 LDFLAGSF= `pkg-config --libs libDesktop`
-LDFLAGS	= 
+LDFLAGS	= -lintl
 RM	?= rm -f
 LN	?= ln -f
 MKDIR	?= mkdir -p
