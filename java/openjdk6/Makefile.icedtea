# $FreeBSD: java/openjdk6/Makefile.icedtea 337396 2013-12-24 23:11:15Z jkim $

_PATCHES=

.if ${PORT_OPTIONS:MICEDTEA}
_PATCHES+= \
	applet_hole.patch \
	jpegclasses.patch
.endif

.for p in ${_PATCHES}
ICEDTEA_PATCHES+=	${FILESDIR}/icedtea/${p}
.endfor
