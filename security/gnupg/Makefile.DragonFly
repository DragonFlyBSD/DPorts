# This option expects USB-1
# Either patch makefile or disable option
#OPTIONS_EXCLUDE+=	SCDAEMON

# stage changed behavior.  Instead of excluding, set option to off by
# default until stage can be fixed.
OPTIONS_DEFAULT:= ${OPTIONS_DEFAULT:NSCDAEMON}
