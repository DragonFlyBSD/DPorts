--- configure.orig	2013-02-14 19:17:50.000000000 +0000
+++ configure
@@ -2363,6 +2363,7 @@ case $host_os in
   darwin*) template=darwin ;;
     dgux*) template=dgux ;;
  freebsd*) template=freebsd ;;
+dragonfly*) template=freebsd ;;
     hpux*) template=hpux ;;
     irix*) template=irix ;;
    linux*|gnu*|k*bsd*-gnu)
