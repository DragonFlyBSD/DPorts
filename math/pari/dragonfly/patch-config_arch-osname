--- config/arch-osname.orig	2014-02-26 13:00:32 UTC
+++ config/arch-osname
@@ -20,7 +20,7 @@ if test -n "$myuname"; then
             arch=`uname -m`
             if test -z "$arch"; then arch=ix86; fi
             ;;
-  freebsd|netbsd|openbsd)
+  freebsd|netbsd|openbsd|dragonfly)
             arch=`uname -m`
             if test -z "$arch"; then arch=ix86; fi
             case $arch in
