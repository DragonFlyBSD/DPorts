--- core/CMakeLists.txt.intermediate	2021-12-21 13:38:29.000000000 +0000
+++ core/CMakeLists.txt
@@ -509,7 +509,7 @@ set(have_plugins "${have_plugins}")
 set(have_afs "")
 
 if(${acl})
-  if(NOT HAVE_WIN32)
+  if(NOT HAVE_WIN32 AND NOT ${CMAKE_SYSTEM_NAME} STREQUAL DragonFly)
     if(${HAVE_SYS_ACL_H})
       set(HAVE_ACL 1)
       message(STATUS "acl found, libs: ${ACL_LIBS}")
