--- qtbase/configure.orig	2015-10-13 04:35:07 UTC
+++ qtbase/configure
@@ -2791,6 +2791,9 @@ if [ -z "$PLATFORM" ]; then
      ULTRIX:*)
         PLATFORM=ultrix-g++
         ;;
+     DragonFly:*)
+        PLATFORM=freebsd-g++
+        ;;
      FreeBSD:*)
         PLATFORM=freebsd-g++
         PLATFORM_NOTES="
