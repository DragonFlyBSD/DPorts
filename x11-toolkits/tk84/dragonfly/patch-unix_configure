--- configure.intermediate	2013-06-19 13:39:38.329390000 +0000
+++ configure
@@ -2922,7 +2922,7 @@ echo "$ac_t""$tcl_cv_ld_elf" 1>&6
 		;;
 	    esac
 	    ;;
-	FreeBSD-*)
+	FreeBSD-*|DragonFly-*)
 	    # This configuration from FreeBSD Ports.
 	    SHLIB_CFLAGS="-fPIC"
 	    SHLIB_LD="${CC} -shared"
@@ -3770,6 +3770,7 @@ fi
 	    CYGWIN_*|MINGW32_*) ;;
 	    IRIX*) ;;
 	    NetBSD-*|FreeBSD-*|OpenBSD-*) ;;
+	    DragonFly-*) ;;
 	    Darwin-*) ;;
 	    RISCos-*) ;;
 	    SCO_SV-3.2*) ;;
