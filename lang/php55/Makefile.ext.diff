--- Makefile.ext.orig	2013-09-12 16:55:27.000000000 +0000
+++ Makefile.ext
@@ -334,6 +334,7 @@ CONFIGURE_ARGS+=--enable-shmop
 CONFIGURE_ARGS+=--enable-simplexml \
 		--with-libxml-dir=${LOCALBASE}
 
+CFLAGS+=	-I${LOCALBASE}/include
 USES+=		pkgconfig
 USE_GNOME=	libxml2
 .endif
