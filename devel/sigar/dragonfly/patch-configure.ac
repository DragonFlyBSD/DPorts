--- configure.ac.orig	2014-11-17 21:46:20 UTC
+++ configure.ac
@@ -47,7 +47,7 @@ case $host_os in
      AC_DEFINE(SIGAR_TEST_OS_DARWIN, [1], [for the tests])
      SIGAR_LIBS="-lkvm"
      ;;
-     *freebsd*)
+     *freebsd*|*dragonfly*)
      SRC_OS="darwin"
      AC_DEFINE(SIGAR_TEST_OS_DARWIN, [1], [for the tests])
      SIGAR_LIBS="-lkvm"
